import React from "react";

export default class AddModal extends Comment {
    constructor(props) {
        super(props);
    }

    render() {
        <div>
            <h1>Add Modal!</h1>
        </div>
    }
}